/node_modules
/package-lock.json

# results.json is COMMITTED: it is the timing source-of-truth bench:verify reads (the
# stored, machine-independent baseline -- verify does no fresh timing run). Regenerate with
# `npm run bench:report`. Only the per-run generated logs below are ignored.
/benchmark/report.html
/benchmark/soak.jsonl
